# Example keystore.properties for signing the Android release build
# Copy this file to `keystore.properties` (same folder as the top-level build.gradle) and fill values,
# or set the equivalent environment variables listed below.

# Path to the keystore file (relative to the project root or absolute path). Defaults to android.keystore if omitted.
storeFile=android.keystore
# Password for the keystore
storePassword=your-keystore-password
# Key alias inside the keystore
keyAlias=android
# Password for the key alias
keyPassword=your-key-password

# Alternative: use environment variables on CI
# KEYSTORE_FILE, KEYSTORE_PASSWORD, KEY_ALIAS, KEY_PASSWORD
